{
  "name": "@jobsradar/repository-postgres",
  "private": true,
  "version": "0.1.0",
  "description": "SearchRepositoryPort con postgres.js, sin ORM (AD-07). Ver ARCHITECTURE.md sección 8.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured yet\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@nearcodecr/jobsradar-contracts": "workspace:*",
    "@jobsradar/domain": "workspace:*",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
