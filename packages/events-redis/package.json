{
  "name": "@jobsradar/events-redis",
  "private": true,
  "version": "0.1.0",
  "description": "EventPublisherPort vía Redis pub/sub + subscribeToSearch para SSE. Ver ARCHITECTURE.md sección 5.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured yet\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@nearcodecr/jobsradar-contracts": "workspace:*",
    "@jobsradar/domain": "workspace:*",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
