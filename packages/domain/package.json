{
  "name": "@jobsradar/domain",
  "private": true,
  "version": "0.1.0",
  "description": "Value Objects, entidades, puertos y Result. Ver ARCHITECTURE.md secciones 4.2 y 5.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured yet\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@diegosancheznearcode/contracts": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
