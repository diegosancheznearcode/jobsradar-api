{
  "name": "@jobsradar/adapter-wellfound",
  "private": true,
  "version": "0.1.0",
  "description": "Adaptador Wellfound (parsers + HttpClient/BrowserClient). Ver ARCHITECTURE.md sección 6.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured yet\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@nearcodecr/jobsradar-contracts": "workspace:*",
    "@jobsradar/domain": "workspace:*",
    "cheerio": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "msw": "^2.15.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
