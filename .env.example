PORT=3000
DATABASE_URL=postgres://jobsradar:jobsradar@localhost:5432/jobsradar
REDIS_URL=redis://localhost:6379
ALLOWED_ORIGINS=http://localhost:5174

# Sesión de Wellfound (sección 6.2) — nunca commitear el archivo real.
# WELLFOUND_STORAGE_STATE_PATH=./storageState.json

# Fallback de LinkedIn (pedido explícito del usuario, sección 9 resultado
# Fase 12) — https://talentradar-api-tnxrhfijdq-pv.a.run.app/api/empresa-linkedin.
# Opcional: sin estas dos variables, company-detail sigue enriqueciendo
# normalmente, solo que linkedinUrl queda en null cuando Wellfound no lo
# trae. Nunca commitear las credenciales reales.
# TALENTRADAR_API_USER=
# TALENTRADAR_API_PASSWORD=
