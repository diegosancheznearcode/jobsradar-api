{
  "name": "@jobsradar/worker",
  "private": true,
  "version": "0.1.0",
  "description": "Consumidores BullMQ (search-list, job-detail, company-detail). Ver ARCHITECTURE.md sección 10.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured yet\"",
    "test": "echo \"no tests yet — Fase 5\""
  },
  "dependencies": {
    "@jobsradar/contracts": "workspace:*",
    "@jobsradar/domain": "workspace:*",
    "bullmq": "^5.13.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
